import { call, takeEvery, put } from 'redux-saga/effects';
import { initialize } from 'redux-form';

import mailConfigService from '../../services/preference/mailConfigService';


// Actionとその発生時に実行するメソッドをリンクさせる
const mailConfigSagas = [

];

export default mailConfigSagas;