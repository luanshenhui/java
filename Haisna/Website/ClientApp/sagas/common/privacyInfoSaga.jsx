import { call, takeEvery, put } from 'redux-saga/effects';
import { initialize } from 'redux-form';

import privacyInfoService from '../../services/common/privacyInfoService';


// Actionとその発生時に実行するメソッドをリンクさせる
const privacyInfoSagas = [

];

export default privacyInfoSagas;