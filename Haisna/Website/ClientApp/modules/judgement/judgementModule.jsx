import { createActions, handleActions } from 'redux-actions';

// actionの作成
//export const {
 
//} = createActions(
 
//);

// stateの初期値
const initialState = {
 

};

// reducerの作成
export default handleActions({
  
}, initialState);
