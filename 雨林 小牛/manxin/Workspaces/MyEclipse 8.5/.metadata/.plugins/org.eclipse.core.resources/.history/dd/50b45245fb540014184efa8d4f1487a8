package com.yulin.am;

import java.util.*;
public class Celen {

	/**
	 * @param args
	 */
	public void qingJie(){
		Scanner scan = new Scanner(System.in);//´´½¨É¨ÃèÒÇ
		String name=scan.next();
		final Pet pet = new Pet(name);
		new Timer().schedule(new TimerTask(){
			@Override
			public void run(){
				pet.level+=10;
				pet.an-=10;
				if(pet.level>=100){
					
				}
			}
		},5000, 1000);
		
	} 

}
