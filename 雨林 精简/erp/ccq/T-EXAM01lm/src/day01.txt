awt
	Frame
swing
	JFrame
	界面中的各个元素被称为组件。
	容器型的组件：里面可以放其他的组件，包括其他容器组件。
		根容器：JFrame、JDialog
	非容器型的组件：里面不可以放其他的组件。
		
MVC设计模式
	M(Model)模型层:干活的人和干活时需要的工具，包括干的活。
	V(View)视图层:看到的页面，接受用户的操作并传达给控制层。
	C(Controller)控制层:负责分配工作。
	
	Control:视图层需要调用Control，Control也需要调用视图层。