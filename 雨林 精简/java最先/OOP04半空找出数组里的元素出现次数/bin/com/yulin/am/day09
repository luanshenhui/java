1Object类；java所有类的
   需要被覆盖的方法；
                  1public String toString()；//输出对象时调用
                  2public boolean equals(Object)；//比较两个对象时调用
                  3public inthashCode()；//返回对象的特征码(散列码)，配合equals用(规范)。
                  
抽象类，接口；
            1抽象类；
               a用Abstract关键字修饰的类。
               b抽象类无法实例化，无法创造对象。
               c抽象类中可以有抽象方法
                         只有方法的修饰和方法的签名，没有方法体
                         public abstract  int add()；
                         抽象方法只能定义在抽象类中。
               
               d抽象类存在的意义就是被继承
               e抽象类定义类的规范。
               f抽象类中等抽象方法，必须被覆盖  
               
            2,接口；
                a.用interface声明
                b.接口中所有的方法都必须是抽象方法
                c.接口中所有的成员变量都必须是常量
                d.接口存在的意义是定义某种规范，只有被实现(implements)才能使用
                e.接口被实现后，必须覆盖所有的抽象方法。
                f.同一个类可以同时实现多个接口。每个接口之间用“，”连接。          
                
练习；1，查询String API完成下列需求
输入一个由0-9组成的字符串，统计每个字符串出现的次数。
升级；输入一个字符串，统计每个字符串出现的次数。
2，输入一个汉字的字符串，如果里面有共产党则换成“***”
查询Integer和Number API 完成一下
1输入一个字符串，取出其中所有的整数，计算他们的和
1，输出字符串String str =“1+2+3+4”的结果
                                         