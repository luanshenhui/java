$.Apollo = {
    domain: {
        IMS_PATH: "${ims.address}",
        API_PATH: "${sys.address}"
    },
    path:{
        API_GET_USER_DESKTOP:"/auth/desktop/getUserDesktop",
        API_GET_USERMANAGE_PLATFORM:"/auth/user/getUserManagePlatform"
    }
}
