<div class="modal specials-item-model">
    <div class="modal-header">
        <a href="javascript:void(0)" class="close">×</a>
        <h2>{{title}}</h2>
    </div>
    <form class="form form-aligned add-specials-form">
        <div class="modal-body">
            <div class="control-group">
                <label for="goodsBrandId">品牌ID：</label>
                <input name="typeId" type="text" value="{{id}}" readonly="readonly"/>
            </div>
            <div class="control-group">
                <label>5位单品编码：</label>
                <input name="goodsId" type="text" required maxlength=5 />
                <span class="note-error"><i>&times;</i> 必填 不得为空</span>
                <span class="note-error-empty"><i>&times;</i> 必填 不得为空</span>
            </div>
            <div class="control-group">
                <label>显示顺序：</label>
                <input name="commendSeq" type="text" required maxlength=9 pattern="^[0-9]\d*$"/>
                <span class="note-error"><i>&times;</i> 请输入正整数</span>
                <span class="note-error-empty"><i>&times;</i> 必填 不得为空</span>
            </div>
        </div>
        <div class="modal-footer">
            <input type="submit" class="btn btn-success btn-medium save-review" value="确定">
            <button type="button" class="btn btn-close btn-medium close">取消</button>
        </div>
    </form>
</div>
